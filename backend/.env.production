# PRODUCTION - Single Root Deployment on Railway
# Railway provides MONGO_URI automatically via MongoDB plugin
# Do NOT set MONGO_URI manually - Railway injects it when MongoDB plugin is added

JWT_SECRET=change-this-to-secure-random-key-in-production
NODE_ENV=production
PORT=5000
